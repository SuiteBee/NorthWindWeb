import React from "react";

const Logout = () => {
    return (
        <>
            <h1>Logout</h1>
            <div>
                This is an intermediate page where the user will be sent back to login
            </div>
        </>
    );
};

export default Logout;